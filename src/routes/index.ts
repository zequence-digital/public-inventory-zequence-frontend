const authRoutes = [
  "/auth/login",
  "/auth/register",
  "/auth/forgot-password",
  "/auth/reset-password",
];

const publicRoutes = ["/"];

const privateRoutes = ["/dashboard"];

export { authRoutes, privateRoutes, publicRoutes };
